// Crear un arreglo con nombres de frutas
let frutas = ["Manzana", "Banana", "Naranja", "Uva", "Mango", "Piña", "Fresa"];

// Usar un bucle para imprimir cada nombre de fruta
for (let i = 0; i < frutas.length; i++) {
    console.log(frutas[i]);
}
