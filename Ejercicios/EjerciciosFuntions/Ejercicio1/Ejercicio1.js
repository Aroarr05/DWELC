//
/**
 * 
 * In an array there are names of people. Create an arrow function that generates a
 *  greeting for each of them.
 * 
 */

const nombres = ["Ana","Lucas","Pablo","Maria"];

const saludo = nombres.map(nombre =>`Holaaa, ${nombre}`);

//Para que se muestre 
console.log(saludar);